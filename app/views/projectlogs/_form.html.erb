<%= form_for(@projectlog) do |f| %>
  <% if @projectlog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projectlog.errors.count, "error") %> prohibited this projectlog from being saved:</h2>

      <ul>
      <% @projectlog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :action %><br>
    <%= f.text_field :action %>
  </div>
  <div class="field">
    <%= f.label :actiondata %><br>
    <%= f.text_area :actiondata %>
  </div>
  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
